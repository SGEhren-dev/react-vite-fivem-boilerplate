export enum RequestMethod {
	POST = "post",
	GET = "get",
	PATCH = "patch",
	DELETE = "delete"
}

export enum PlayerStat {
	HEALTH = "HEALTH",
	ARMOR = "ARMOR",
	BREATH = "BREATH"
}

export enum TimeUnits {
	SECONDS = 1000,
	MINUTES = 60000
}
